<!doctype html>
<html>
  <head>
    <meta name="viewport" content="width=device-width" />
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title><%= custom.subject %></title>
    <style>
      body {
        font-family: sans-serif;
        background-color: #f6f6f6;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 580px;
        margin: 20px auto;
        background: #ffffff;
        padding: 20px;
        border-radius: 4px;
      }
      .logo {
        text-align: center;
        margin-bottom: 20px;
      }
      .content p {
        font-size: 14px;
        line-height: 1.5;
        margin: 10px 0;
      }
      h2 {
        text-align: center;
        color: #333;
        font-size: 18px;
        margin-top: 0;
      }
      .footer {
        text-align: center;
        font-size: 12px;
        color: #999999;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="logo">
        <img
          src="<%= email_logo %>"
          alt="<%= site_title %>"
          style="width: 120px; height: auto;"
        />
      </div>

      <% if (custom.subject) { %>
        <h2><%= custom.subject %></h2>
      <% } %>

      <div class="content">
        <p>Hello,</p>

        <p>Welcome to <strong><%= site_title %></strong>! We’re excited to have you on board.</p>

        <% if (custom && custom.password) { %>
          <p>Your account has been created successfully.</p>
          <p><strong>Password:</strong> <%= custom.password %></p>
          <p>We recommend changing your password after logging in for security purposes.</p>
        <% } else { %>
          <p>You can now log in using your credentials. If you haven’t set a password yet, please check your app for setup instructions.</p>
        <% } %>

        <p>Feel free to explore and let us know if you have any questions.</p>

        <p>— The <%= site_title %> Team</p>
      </div>
    </div>

    <div class="footer">
      &copy; <%= new Date().getFullYear() %> <%= site_title %>. All rights reserved.
    </div>
  </body>
</html>
