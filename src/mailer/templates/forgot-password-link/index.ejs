<!doctype html>
<html>
  <head>
    <meta name="viewport" content="width=device-width" />
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title><%= custom.subject || "Reset Your Password" %></title>
    <style>
      body {
        font-family: sans-serif;
        background-color: #f6f6f6;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 580px;
        margin: 20px auto;
        background: #ffffff;
        padding: 20px;
        border-radius: 4px;
      }
      .logo {
        text-align: center;
        margin-bottom: 20px;
      }
      .content p {
        font-size: 14px;
        line-height: 1.5;
        margin: 10px 0;
      }
      h2 {
        text-align: center;
        color: #333;
        font-size: 18px;
        margin-top: 0;
      }
      .button {
        display: block;
        width: max-content;
        background-color: #007bff;
        color: #ffffff;
        text-decoration: none;
        padding: 10px 20px;
        border-radius: 4px;
        text-align: center;
        margin: 20px auto;
        font-weight: bold;
      }
      .footer {
        text-align: center;
        font-size: 12px;
        color: #999999;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="logo">
        <img
          src="<%= email_logo %>"
          alt="<%= site_title %>"
          style="width: 120px; height: auto;"
        />
      </div>

      <% if (custom.subject) { %>
        <h2><%= custom.subject %></h2>
      <% } else { %>
        <h2>Password Reset Request</h2>
      <% } %>

      <div class="content">
        <p>Hello,</p>

        <p>We received a request to reset your password for your <strong><%= site_title %></strong> account.</p>

        <% if (custom && custom.resetLink) { %>
          <p>Click the button below to reset your password:</p>

          <a href="<%= custom.resetLink %>" class="button">Reset Password</a>

          <p>If the button doesn't work, copy and paste this link into your browser:</p>
          <p><a href="<%= custom.resetLink %>"><%= custom.resetLink %></a></p>
        <% } else { %>
          <p>No reset link provided. Please contact support.</p>
        <% } %>

        <p>If you didn't request a password reset, please ignore this email.</p>

        <p>— The <%= site_title %> Team</p>
      </div>
    </div>

    <div class="footer">
      &copy; <%= new Date().getFullYear() %> <%= site_title %>. All rights reserved.
    </div>
  </body>
</html>
